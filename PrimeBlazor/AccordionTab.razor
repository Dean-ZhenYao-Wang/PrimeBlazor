@code {
    [Parameter] public string header { get; set; } = string.Empty;
    [Parameter] public bool active { get; set; }
    [Parameter] public EventCallback<bool> activeChanged { get; set; }
    [Parameter] public bool disabled { get; set; }
    [Parameter] public RenderFragment<bool>? ChildContent { get; set; }
    [Parameter] public RenderFragment? Headertemplate { get; set; }
    [CascadingParameter] public Accordion? AccordionSet { get; set; }
    protected override void OnInitialized()
    {
        if (AccordionSet is not null)
        {
            if (!AccordionSet.IsTabAny(this))
            {
                AccordionSet.addTab(this);
            }
        }
    }
}
